# 后端缺口清单

这份清单用于记录已完成前端页面中，暂时仍需要后端接口、数据库或真实业务流程支持的部分。每完成一个界面就追加对应条目，后续统一补齐并逐项验收。

## 后端清单审计（2026-09-06）

- 当前清单共有 3 项未完成项、79 项已确认完成项；未完成项主要来自真实消息供应商、外部同步和法律文本审批。
- 当前 API 已具备认证、画像、偏好、对话、反馈、知识库、专业目录、推荐、培养方案和岗位详情等基础端点；`/api/health` 当前可正常返回。
- 当前后端仍以 MVP 为主：部分业务数据来自内存常量或前端状态，课程与岗位缺少完整真实时序/业务状态，通知与外部同步仍未接入供应商，正式教务动作和管理端流程仍需继续完善。
- 审计发现 Docker Compose 没有显式传入 `MOCK_LLM=true`，而配置默认值为 `false`，存在误启用真实模型的风险；本阶段已改为 Compose 显式传入且配置默认使用 mock。
- 本轮已完成通知 worker、验证码 provider、反馈管理流转和计划导出历史，并新增无外部服务依赖的后端契约测试；剩余项目保留在下方逐项推进。

### 建议实施顺序

1. **P0：运行与安全底座**——固定 mock 配置；统一密码策略并轮换旧 token；补充输入校验、管理员权限、文件上传限制、数据删除和关键操作审计；将数据库初始化从 `create_all` 逐步收敛到迁移流程。
2. **P1：账号数据与核心业务持久化**——结构化画像/偏好 schema、引导完成状态、头像、联系方式、岗位收藏、学习记录、隐私设置、目标培养方案和冲突策略。
3. **P1：推荐与课程规划真实化**——将评分规则和先修要求配置化，补充课程开设学期/时间/校区/容量，生成并保存可追溯的推荐报告和培养方案。
4. **P2：职业与同步服务**——岗位来源更新、岗位详情扩展、投递状态、数据同步任务、缓存清理、通知任务和反馈工单。
5. **P2：知识库与伴学增强**——文档版本有效期、上传存储、检索质量评估、知识图谱数据库化、知识点学习记录和上下文问答。

### 审计边界

- 审计阶段核对了仓库代码、迁移文件、前端 API 调用、OpenAPI 路由和本地健康检查；随后已按审计结论先落地 P0 的 mock 与认证安全修复。
- 当前本地后端自测脚本未能通过系统 Python 启动器执行，原因是本机 Python 启动路径编码异常；CI 仍配置为执行 `scripts/verify_core.py`，后端正式改动前需要在 Docker 或 CI 环境补跑完整验证。

## 绑定手机/邮箱

- [ ] 联系方式的真实短信/邮件供应商投递和送达回执接口、provider message_id、回调密钥和重试已完成；生产供应商凭据与供应商侧模板/合规验收仍待接入。更换主联系方式、备用联系方式及申请冷却限流已完成。
- [x] 服务端已新增联系方式持久化、脱敏查询、主联系方式切换和删除接口；验证码发送/校验仍待接入真实服务。
- [x] 服务端已新增验证码挑战的摘要存储、10 分钟过期、最多 5 次尝试、一次性消费、申请冷却、provider message_id、投递重试和签名回调状态记录；真实短信/邮件 provider 凭据与供应商模板仍待配置。
- [x] 主联系方式切换现要求联系方式已验证；验证成功后的默认主联系方式和删除主联系方式后的备用提升由服务端处理。

## 个人主页

- [x] `/learning/progress` 已返回真实已修学分、采用培养方案目标学分和完成百分比；前端仍待接入替换占位 `28/50`。
- [x] 服务端已提供账号级学习活动打卡记录，并在 `/learning/progress` 返回学习天数和连续学习天数；前端仍待接入。
- [x] 服务端已新增学习记录持久化和按课程状态汇总学分的 `/learning/progress` 接口；前端仍待接入并补充学习打卡统计。
- [x] 服务端已新增账号级岗位收藏表及查询、添加、取消接口；前端仍待切换到服务端收藏。

## 花园首页

- [x] 服务端新增账号级方向加入/退出记录，并按辅修方向返回真实参与人数。
- [x] `/programs` 已返回培养方案课程数和参与人数，前端可据此替换固定的方向热度与课程数量展示。

## 岗位详情

- [x] 当前通过 `/jobs/{id}` 读取真实岗位详情，并支持收藏和有在线链接时跳转投递。
- [x] 岗位详情接口已返回远程方式、岗位类型、到岗时间、实习时长、职责、投递邮箱和投递备注等设计稿字段；旧 Docker 数据卷会在启动时补列。
- [x] 岗位收藏已提供账号级接口并按账号隔离；前端从浏览器本地存储切换到服务端及跨设备同步仍待接入。
- [x] 服务端已提供岗位收藏接口并按账号隔离，前端仍待切换并补充跨设备同步状态。
- [x] 服务端已提供账号隔离的岗位跟进记录，支持感兴趣、已投递、筛选、面试、录用、拒绝和撤回状态，以及渠道、备注和时间记录；外部招聘渠道回流和提醒任务仍待接入。

## 辅修推荐

- [x] 推荐接口会读取已修课程记录判断高数先修状态，并提供 `/programs/{program_name}/prerequisites` 查询依据；前端提示仍待接入。
- [x] 推荐结果已同时返回方向参与人数和基于画像评分规则生成的可追溯解释文本。

## 辅修推荐分析报告

- [x] 当前通过 `/recommend` 和 `/programs/plan` 读取推荐结果及培养方案，报告页展示综合匹配、分项评分、课程、学分和风险提示。
- [x] 服务端已保存推荐报告版本、生成时间、画像版本、画像快照和推荐结果，并提供历史报告查询接口；前端历史报告入口仍待补齐。
- [x] 截图中的就业出口、岗位方向和规则化难度已有接口；真实就业率、院系统计和官方去向仍不虚构，待接入权威数据。
- [x] 已提供 `/programs/{program_name}/career-outcomes`，按岗位表与知识图谱技能映射返回关联岗位数、岗位标题、技能和规则化难度；真实就业率、院系统计和官方去向仍未接入。
- [x] 服务端已提供 `/programs/{program_name}/eligibility`，返回已修学分、已导入课程、缺失课程、高数先修证据和保守的资格估算；正式教务/院系审核仍明确保留为人工复核。

## 土壤条件画像

- [x] 当前已通过 `/profile` 接口读取和保存主修专业、兴趣方向和能力特长。
- [x] 主修专业已改为通过 `/programs` 接口从数据库专业目录加载。
- [x] 服务端提供版本为 1 的统一兴趣/能力字典，并在画像保存时校验合法项、去重；历史选项迁移仍需在后续版本变更时补充。
- [x] 画像修改后会将该账号尚未失效的推荐报告标记为过期；重新计算接口会生成新的可追溯报告。
- [x] 画像已记录版本和更新时间，并随推荐报告保存画像快照，支持结果追溯和账号级同步。
- [x] 注册引导完成状态已提供账号级 `/auth/onboarding` 读写接口；前端从本地状态切换到服务端仍待接入。
- [x] 服务端已提供账号级引导状态字段及 `/auth/onboarding` 读写接口；前端尚未切换到该接口。

## 编辑画像

- [x] 编辑画像页已通过 `/profile` 和账号昵称接口读取、保存基础画像；保存时采用局部合并，不覆盖已有排课及冲突偏好。
- [x] 主修专业复用数据库专业目录，避免编辑页继续使用自由文本造成专业名称不一致。
- [x] 已提供 `/profile/avatar` 头像上传、账号级存储、读取和删除接口，并限制 JPG/PNG/WebP、2 MB；裁剪、对象存储和跨端 CDN 仍待接入。

## 关于福小禾

- [x] 已按设计稿展示品牌标志、版本号、产品定位、建设信息和功能入口说明。
- [x] 版本号、诞生年份、开发团队及功能清单已集中到 `/meta/product`；构建流水线注入版本、更新日志和产品配置管理仍待接入。
- [x] 已提供 `/meta/product` 集中返回版本、诞生年份、团队和功能目录；构建流水线注入版本、更新日志和产品配置管理仍待接入。
- [x] 已提供 `/meta/legal` 返回服务条款、隐私和开源版权说明草案；正式法律文本、许可证清单和前端展示仍待项目方审核接入。

## 排课时间偏好

- [x] 当前已复用 `/profile` 接口，通过 `schedule_preferences` 字段读取和保存能量时段、上课时间段、课程集中度、午休习惯及晚间偏好标记。
- [x] `schedule_preferences` 已收敛为结构化字段 schema，校验时间段、集中度、午休、晚间偏好和列表范围，并提供默认值。
- [x] 时间偏好已接入自动排课候选排序；正式教务选课、能量时段的更细颗粒约束仍待接入。
- [x] 排课方案已支持生成记录、草稿/采用状态和按最新画像重新计算；前端入口和自动触发仍待接入。
- [x] 已提供 `/programs/plans/{plan_id}/schedule-preview`，将实际教学班、时间重叠、校区通勤风险和冲突策略分析保存回课程规划；能量时段与课程容量等更细约束仍待接入。

## 辅修冲突处理

- [x] 当前已复用 `/profile` 接口，通过 `schedule_preferences.conflict_strategies` 读取和保存用户选择的冲突处理策略。
- [x] 冲突策略已纳入画像的结构化 schema，校验支持的策略枚举和最多五项范围；策略优先级仍待单独建模。
- [x] 服务端已保存选课数据中的教学星期/节次，并提供真实时间重叠、校区通勤风险和可行性分析接口；免修、线上课程和课程替换仍待接入。
- [x] 已提供自动选班、冲突策略排序和备选组合返回；自动提交选课和免修/替换等正式策略执行仍待接入。
- [x] 已将冲突分析结果持久化到课程规划快照，并返回缺失教学班和画像版本，支持前端重新计算。

## 课程规划

- [x] 当前通过 `/programs` 和 `/programs/plan` 读取真实专业目录及结构化培养方案，前端按培养阶段和学期展示课程。
- [x] 服务端已提供账号级课程规划快照、草稿/采用/归档状态、计划调整、历史查询和账号隔离接口；前端仍待接入保存与采用动作。
- [x] 已提供 `/programs/plans/{plan_id}/recalculate`，按最新画像重新生成保存计划并清除过期排课分析；前端入口和自动触发仍待接入。
- [x] 课程冲突提示和备选方案已按真实教学班动态生成；更细的教务规则和正式选课动作仍待接入。
- [x] 已提供 `/programs/{name}/schedule-options`，按培养方案课程返回实际教学班候选、校区优先级和未开设课程列表；容量、能量时段和自动排课仍待接入。
- [x] 教学班已增加容量与已选人数，并在候选排序中标记/后置满班；容量数据导入和自动排课仍待接入。
- [x] 已提供 `/programs/{name}/schedule-auto`，结合校区、时间段、冲突策略和班级容量从候选教学班中自动选择组合，并明确返回冲突、满班和缺课提示；自动提交选课仍待接入。
- [x] 后端已提供可追溯导出记录和 ICS/JSON/CSV 格式；前端仍需从浏览器文本导出切换到该接口，真实教学日期和教务/系统日历同步仍待接入。
- [x] 已提供 `/programs/plans/{plan_id}/export` 的 JSON/CSV/ICS 导出，并将每次导出的格式、计划、账号、状态和内容摘要写入 `/programs/exports`；真实教学日期和教务/系统日历同步仍待接入。

## 漫步知识森林

- [x] 服务端已按账号和课程范围返回知识点树、节点完成状态及汇总进度，并支持知识点状态更新；资源关联仍待补齐。
- [x] 已增加 `/knowledge/nodes/{node_id}/resources` 资源元数据接口，并修复知识点到技能/职业的映射；真实文档内容和资源检索仍待接入。
- [x] `/knowledge/pathways` 已返回在校辅修/独立自学模式的路径提示，并通过 `/knowledge/pathways/mode` 保存账号选择；两种模式的真实课程/自学任务生成仍待接入。
- [x] 已提供 `/knowledge/progress`，按知识点进度、学习打卡天数和已修学分返回学习生长汇总；更细的阶段任务和目标培养方案关联仍待接入。
- [x] 伴学问答已有 `/chat` 流式接口，知识森林页保留了进入问答的入口。
- [x] ChatRequest 和伴学 Agent 已接收知识点 ID/名称并注入问答上下文；关联资源列表接口已补齐，真实文档关联仍待补齐。

## 设置及其子页面

- ### 通知设置

- [x] 当前已通过 `/preferences` 接口按账号读取和保存通知开关。
- [x] 偏好接口已增加版本化 schema，并校验通知、提醒、岗位推送和可见范围字段。
  - [x] 通知、学习提醒、岗位推送和可见范围偏好已收敛为版本化字段 schema，服务端提供默认值并校验枚举、布尔值、时间和列表范围。
  - [x] 已统一通知开关默认值，并接入学习提醒、推荐报告完成、排课冲突预警三个后端事件；岗位和政策事件及真实外部发送服务仍待接入。
  - [x] 站内通知到期后会由服务端推进为 `sent`，读取列表时返回真实投递状态；外部渠道由独立 worker 按 provider 配置投递，未配置 provider 时保持 `queued`。
  - [x] 服务端已增加站内通知队列、账号隔离查询、未读数、已读状态、外部 provider payload、指数退避重试和 worker 调度；真实渠道凭据及供应商合规验收仍属于部署接入项。

- [x] 服务端提供 `/preferences/learning-reminder` 分区接口，按账号保存提醒时间、频次和星期范围；通知 worker 会按自然日去重生成到期站内提醒，读取列表仍保留兜底生成逻辑。
- [x] 服务端提供 `/preferences/job-push` 分区接口，按账号保存岗位类型、城市和行业筛选；真实推送服务仍待接入。
- [x] 学习进度可见范围已按账号保存细项开关，并由公开摘要接口执行本人、同专业和全校公开的访问范围控制。
- [x] 服务端已提供 `/profiles/{user_id}/public-summary`，按“仅自己/仅好友/同专业同学/全校公开”执行访问判断，并按细项开关裁剪课程、成绩和时间偏好；好友关系数据模型仍待接入。
- [ ] 数据同步：服务端已提供账号级同步检查点、脱敏快照导出、provider 推送/拉取、预览/确认导入、范围、版本、状态、稳定同步指纹、`remote_wins`/`keep_local` 冲突策略和自动调度 worker；provider 返回的跨端快照仍需经过预览/确认导入，备份账号托管仍待接入。
- [x] 同步请求已增加请求幂等键、重试计数和状态返回，网络重试不会重复推进同一批次版本；外部 provider 推送/拉取均保留确认导入边界。
- [x] 清除缓存：服务端已提供 `/cache/status` 和 `/cache/clear`，按账号记录图片、对话、推荐缓存命名空间的失效代次和时间，并明确不会删除业务数据；真实 Redis/对象存储缓存消费者及大小统计仍待接入。
- [x] 已提供 `/auth/accounts` 账号列表和账号关联关系，切换接口会验证已建立关联后签发目标账号新会话；前端仍待接入。
- [x] 修改密码：前端已接入 `/auth/change-password`，成功后清除本地登录态并引导重新登录。
- [x] 修改密码：服务端已统一校验 8-20 位且同时包含字母和数字，改密后会轮换 token 使旧会话失效。
- [x] 修改密码：服务端已保存最近密码摘要，并拒绝复用当前密码和最近五次密码。
- [ ] 修改密码：找回密码接口、一次性验证码消费、申请冷却、可配置验证码 provider 和 worker 重试已完成；生产供应商凭据配置及异地登录风控仍待接入。
- [x] 服务端已增加账号会话摘要、过期/撤销判断、会话列表和单会话撤销；登录会基于已有会话的 IP/User-Agent 标记新环境，生成安全提醒但不误阻断正常换网。
- [x] 已提供基于已验证主联系方式的 `/auth/password-reset/request` 和 `/auth/password-reset/confirm`，成功重置会撤销旧会话并降低账号枚举风险；验证码挑战消费后不可重放，真实 provider 由配置和 worker 接管。
- [x] 意见反馈：前端已接入 `/feedback`，支持反馈类型、描述、联系方式和最多三张图片名称提交。
- [x] 意见反馈：附件已支持服务端上传、签名校验、可选扫描 provider 和可选对象存储 provider，并保存扫描状态、存储后端和 key；管理员已可查询、流转工单并回复用户，回复会生成站内通知且管理端联系方式已脱敏。未配置生产 provider 时明确回退为本地校验/数据库存储。
- [x] 已提供反馈附件上传/读取接口，按反馈账号隔离，限制类型、大小、文件签名和单条反馈数量，并保存 SHA-256 摘要；病毒扫描、对象存储和管理端工单仍待接入。
- [x] 已提供 `/auth/accounts/{account_id}/switch` 切换会话/令牌，以及账号隔离的解除关联接口；前端仍待接入当前账号重新加载。
- [x] 已提供 `/auth/accounts/link`，要求输入目标账号凭据后建立双向账号关联，并支持重复关联幂等处理。

## 记录规则

- 前端页面可以先完成展示和交互占位，但不得把占位数据误认为真实业务数据。
- 后续后端阶段按页面逐项实现接口、持久化、权限校验和异常状态，再回填前端并验收。
